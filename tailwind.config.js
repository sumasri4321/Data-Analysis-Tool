/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // This line tells Tailwind to scan all HTML and TypeScript files in src/ and its subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}